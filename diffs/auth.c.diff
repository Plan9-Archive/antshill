--- /sys/src/9/port/auth.c	Sun Jul  8 10:17:56 2018
+++ ../frontmods/port/auth.c	Wed Oct 31 22:08:50 2018
@@ -141,6 +141,7 @@
 
 	renameuser(eve, buf);
 	srvrenameuser(eve, buf);
+	zrvrenameuser(eve, buf);
 	shrrenameuser(eve, buf);
 	kstrdup(&eve, buf);
 	kstrdup(&up->user, buf);
