--- /sys/src/9/port/auth.c	Thu Mar 10 11:52:45 2016
+++ ../frontmods/port/auth.c	Fri Dec  7 23:24:23 2018
@@ -141,6 +141,7 @@
 
 	renameuser(eve, buf);
 	srvrenameuser(eve, buf);
+	zrvrenameuser(eve, buf);
 	shrrenameuser(eve, buf);
 	kstrdup(&eve, buf);
 	kstrdup(&up->user, buf);
