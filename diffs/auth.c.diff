--- /sys/src/9/port/auth.c	Sat Dec 31 17:31:14 2016
+++ ../frontmods/port/auth.c	Fri Feb 16 12:50:35 2018
@@ -141,6 +141,7 @@
 
 	renameuser(eve, buf);
 	srvrenameuser(eve, buf);
+	zrvrenameuser(eve, buf);
 	shrrenameuser(eve, buf);
 	kstrdup(&eve, buf);
 	kstrdup(&up->user, buf);
