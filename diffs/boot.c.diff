--- /sys/src/9/boot/boot.c	Sun Jul  8 10:17:56 2018
+++ ../frontmods/boot/boot.c	Wed Oct 31 22:08:50 2018
@@ -9,6 +9,7 @@
 {
 	char cputype[64];
 	char buf[32];
+	char bootcmd[64];
 
 	fmtinstall('r', errfmt);
 
@@ -36,6 +37,7 @@
 	USED(argc);
 
 	readfile("#e/cputype", cputype, sizeof(cputype));
+	readfile("#e/bootcmd", bootcmd, sizeof(bootcmd));
 
 	/* setup the boot namespace */
 	bind("/boot", "/bin", MAFTER);
@@ -44,5 +46,7 @@
 	snprint(buf, sizeof(buf), "/%s/bin", cputype);
 	bind(buf, "/bin", MAFTER);
 	bind("/rc/bin", "/bin", MAFTER);
+	if(strcmp(bootcmd, "plan9rc")==0)
+		exec("/boot/plan9rc", argv);
 	exec("/bin/bootrc", argv);
 }
