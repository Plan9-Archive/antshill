#!/bin/rc

rfork
scorefile=/shr/9fat/scorenames
listenport=10564

while(~ $1 -*){
	switch($1){
	case -l
		listenport=$2
		shift
	case -v
		scorefile=$2
		shift
	}
	shift
}

. $scorefile

namefossil -l $listenport $$1 $1 $1
mv /tmp/killscript /tmp/kill.$1
