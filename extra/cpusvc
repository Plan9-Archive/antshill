#!/bin/rc

rfork
while(usercmd=`{read}){
	switch($usercmd){
	case spawn*
		echo spawn not supported on cpu node
	case fetch*
		fetchroot -d $usercmd(2) -p $usercmd(3) -k $usercmd(4) &
	case save*
		echo save
	case clean*
		echo clean
	case exit
		exit
	case *
		echo unknown
	}
}

